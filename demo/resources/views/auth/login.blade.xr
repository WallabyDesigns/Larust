@extends('layouts.app')

@globals
    title = format!("{} | {}", larust_support::lang::t("meta.login_page"), larust_support::lang::t("meta.brand_tagline"))
    cannonical = "/login"
@endglobals

@section('content')
    <main class="page-narrow">
        <section class="form-card">
            <h1>{{ larust_support::lang::t("auth.login_heading") }}</h1>
            <p>{{ larust_support::lang::t("auth.login_lead") }}</p>
            @if(!flash_error.is_empty())
                <p class="flash-error">{{ flash_error }}</p>
            @endif
            <form method="POST" action="/login">
                @csrf
                <div class="field">
                    <label for="email">{{ larust_support::lang::t("auth.email_label") }}</label>
                    <input id="email" type="email" name="email" placeholder="ada@example.com" required>
                </div>
                <div class="field">
                    <label for="password">{{ larust_support::lang::t("auth.password_label") }}</label>
                    <input id="password" type="password" name="password" placeholder="{{ larust_support::lang::t("auth.password_placeholder") }}" required>
                </div>
                <button class="button" type="submit">{{ larust_support::lang::t("nav.log_in") }} <span>&rarr;</span></button>
            </form>
            <p class="form-footer">{{ larust_support::lang::t("auth.new_here") }} <a href="/register">{{ larust_support::lang::t("auth.create_account_link") }}</a></p>
        </section>
    </main>
@endsection
