@extends('layouts.app')

@section('content')
    @globals
        title = "Posts | Larust - ship with confidence"
        canonical = "/posts"
    @endglobals

    <main class="page">
        <div class="page-heading page-actions">
            <div>
                <h1 class="page-title">{{ larust_support::lang::t("posts.heading") }}</h1>
            </div>
            <a class="button" href="/posts/create">
                {{ larust_support::lang::t("posts.write_a_post") }} <span>&rarr;</span>
            </a>
        </div>
        @if(!flash_success.is_empty())
            <p class="flash-success">{{ flash_success }}</p>
        @endif
        @wire('post-list', { tag: tag })
    </main>
@endsection
